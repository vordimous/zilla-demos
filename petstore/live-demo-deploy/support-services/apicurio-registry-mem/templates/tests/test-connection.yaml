apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apicurio-registry-mem.fullname" . }}-test-connection"
  labels:
    {{- include "apicurio-registry-mem.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apicurio-registry-mem.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
