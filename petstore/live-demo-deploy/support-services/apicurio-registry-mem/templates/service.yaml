apiVersion: v1
kind: Service
metadata:
  name: {{ include "apicurio-registry-mem.fullname" . }}
  labels:
    {{- include "apicurio-registry-mem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apicurio-registry-mem.selectorLabels" . | nindent 4 }}
